# Publication intent

This replay branch exists only to authenticate the exact carrier, qualify its 32 public-safe files, replace transport with source, and open the resulting source-only change for review against `main`. The workflow removes this marker with the rest of `carrier/` before publication.
