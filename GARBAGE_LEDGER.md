# Garbage and residue ledger

This ledger names construction residue, namespace pollution, and unfinished work so none of it can quietly re-enter the canon repository.

## Disposable canon-construction residue

The following `axm-arc` pull requests were execution carriers or superseded integration surfaces. Their product authority is zero and their files were not migrated as product source:

- #235, source-health repair trigger;
- #237, structured-adapter repair trigger;
- #240, private-research digest trigger;
- #241, structured-acquisition repair trigger;
- #246, superseded reconciliation integration attempt;
- #299, temporary exact-source export trigger used for migration.

PR #247 remains only as integration lineage. Its duplicate source is represented by the material heads rather than copied as a fourth tree. PR #245 belongs to Aperture and was never part of this migration. The old branch-specific canon and ASOIAF workflows were construction topology rather than product behavior.

## Public-corpus transport cleanup

The public-corpus carrier used attachment URLs, base64 transport, manifests, trigger markers, temporary branches, and write-enabled admission workflows to move an exact public-safe corpus through constrained interfaces. Those objects had transport authority only. The final authenticated admission run reconstructed the carrier, verified its byte counts and SHA-256 identities, rejected unsafe archive structure, reproduced the exact 32-file inventory, ran the complete repository qualification, and created commit `572e2f2cdac58ab2e30072965e63be8f64044549`.

The GitHub Actions token could not update workflow files, so the remote ref update was rejected after the qualified Git objects had reached repository storage. The controlling repair preserved that exact commit on `archive/dangling-qualified-public-corpus-572e2f2`, created `archive/main-pre-corpus-materialization-20260819`, and then fast-forwarded `main` through the repository connector. No corpus file was reconstructed or rewritten during that repair.

The admitted tree removes `.admission-payload/`, `.carrier-input/`, `carrier/`, trigger markers, and the temporary corpus-admission and recovery workflows. Historical transport branches and commits remain lineage only. They have no standing as corpus objects, fictional evidence, canon rules, or continuing production machinery. Reintroducing them requires a new transport need and a new bounded receipt.

## Tier Bench code that was wearing ASOIAF names

PR #249 begins a separate product. Work orders, leases, settlements, persistent desks, workers, exchanges, supervision, authenticated transport, certificate enrollment, credential deployment, broker services, actor runtimes, adapter process custody, Linux capability enforcement, and Windows AppContainer diagnostics are Tier Bench. They are neither lore nor canon.

The affected line includes #249, #250, #251, #252, #253, #254, #255, #256, #257, #258, #260, #264, #265, #270, #273, #275, #278, #279, #280, #282, #284, #288, #294, and #298, together with their temporary carriers and diagnostics. Their historical `asoiaf-answer-*` names are namespace pollution. Any surviving Tier Bench material must be named by mechanism rather than corpus identity.

## Evidence that is not lore

Synthetic fixtures, CI estates, canary responses, workflow logs, artifact receipts, model-recall confidence scores, transport manifests, and repository-state markers qualify mechanisms. They do not establish fictional facts and cannot be promoted into the ASOIAF corpus without exact-source review.

## Private corpus construction residue

The first all-corpus positional index was a 520,409,867-byte nested JSON prototype. It was never repository eligible and was retained privately only long enough to establish `sha256:e88edb36ff9528321c2f3c14032b08d4798714bf802c1b0c4e63a99ce40b239b`. It was removed after the SQLite FTS5 corpus reproduced all 45,828 paragraph documents and passed integrity, count, digest, and query witnesses. Neither the prototype nor its private SQLite replacement belongs in Git.

## Known unfinished corpus work

The six-edition public-safe corpus is materially admitted and the two reader surfaces are published. The five principal novels have not yet been reconciled chapter by chapter. The 805 provisional candidates remain a human review queue, the 31 AGOT opening records remain drafts with zero canon promotions, the continuation-v3 component payloads require bounded admission, and private v4.2 integration remains held on the exact inherited v4.1 archive. Additional orchestration does not close any of those gaps.
