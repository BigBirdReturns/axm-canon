# AGOT repository effect patch builder v1

This directory admits the exact public-safe carrier for `asoiaf-agot-repository-effect-patch-builder-v1.zip`. The ZIP is 19,300 bytes at SHA-256 `fa50113c028738cb61806832d4fdf4e823ec5da12ec99d557315b9a5fd5a41fa` and expands to fourteen files.

The component is the immediate downstream successor to the admitted review-admission and effect-packet gate. It accepts one exact public-safe transaction ledger entry, one independently validated effect packet, one separately named patch-executor authorization, and one exact repository projection snapshot. It can materialize two proposed append-only ledger postimages plus an unapplied unified diff.

The static verifier passes 47 of 47 checks. The six-action synthetic campaign passes 44 of 44 checks across `confirm`, `correct`, `split`, `merge`, `reject`, and `defer`. The campaign contains fixture text only and consumes no real review transaction.

The package, carrier, repository path, workflow, or successful reconstruction cannot execute a repository mutation, open a pull request, promote canon, or mutate the graph. A successful real operation stops at `PASS_REPOSITORY_PATCH_MATERIALIZED_PENDING_HUMAN_PR_REVIEW`.

The controlling question is whether a separately authorized repository executor can prove that the proposed postimages still match the exact target preimages and effect packet before any checkout, branch, commit, or pull-request state changes.
