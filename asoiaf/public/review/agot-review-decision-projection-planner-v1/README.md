# AGOT review decision projection planner carrier v1

This repository surface carries the exact public-safe projection planner that follows the validated-intake transaction stager. The reconstructed ZIP contains a source-independent planner, an independent plan validator, a six-action synthetic campaign, Windows launcher, contracts, receipts, and package checksums.

The exact ZIP is `asoiaf-agot-review-decision-projection-planner-v1.zip`, 19,684 bytes at SHA-256 `89822cb134d14c736b9c41fa4f2e16824bbaf3096a8ed9b817b97dfc95b4500b`. Git stores 6 ordered RFC 4648 base64 chunks totaling 26,248 characters at SHA-256 `6b1e3913ce3c5ed929de34ee252713ee424ecc79f8b8472c769952d6bc2b2fee`. `CARRIER.json` fixes every chunk path, character count, SHA-256, Git blob identity, concatenation order, decoded byte count, and decoded ZIP digest.

The planner accepts one valid sealed transaction candidate, its independent validation receipt, a separately named projection-planning authorization, and the exact forty-five-candidate register. It emits one deterministic `PASS_PROJECTION_PLAN_SEALED_NO_EFFECT_EXECUTED` plan. `confirm`, `correct`, and `split` create proposition and evidence-edge drafts. `reject` creates a rejection disposition. `defer` retains a human-review hold. `merge` retains `HOLD_MERGED_STATEMENT_REQUIRED` because the upstream transaction contains candidate identities rather than separately reviewed merged wording.

Carrier reconstruction proves package identity only. It does not admit the transaction candidate, execute any projection, write a canon ledger, promote canon, or mutate the graph. Real consumed transaction candidates, real projection plans, repository transaction admissions, effect executions, canon promotions, and graph mutations remain zero.
