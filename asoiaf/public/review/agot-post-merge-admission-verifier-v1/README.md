# AGOT post-merge admission verifier v1

This repository surface carries the exact read-only post-merge verifier package. The reconstructed ZIP contains the verification contract, named authorization template, GitHub inspection operator, static verifier, noncopyrighted fake-`gh` campaign, Windows launcher, and qualification receipts.

The package is `asoiaf-agot-post-merge-admission-verifier-v1.zip`, 15,163 bytes at SHA-256 `9dc0ddca7cf11532f0da7255555bccc01653e0bd25e3ee14b0e41bbb318d3aba`. `CARRIER.json` fixes the carrier, Git blob, decoded-object, and twelve-file expansion identities.

The verifier consumes an exact merge receipt, merge-request packet, patch manifest and postimages, and a separately named authorization. It verifies the merge commit, its single pre-merge parent, the exact two changed paths, remote postimage bytes and SHA-256 values, and continued membership in the current base-branch history. The operator is read-only and cannot modify a repository, pull request, branch, canon ledger, or graph ledger.

A successful real operation would stop at `PASS_POST_MERGE_REPOSITORY_ADMISSION_VERIFIED_EFFECT_ACTIVATION_WITHHELD`. Repository admission verification does not activate canon or graph effect. Every real upstream input remains absent.
