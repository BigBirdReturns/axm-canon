# AGOT remote-reference state observer carrier

This directory carries the exact public-safe source package for `asoiaf-agot-remote-reference-state-observer-v1`. Four ordered RFC 4648 base64 chunks reconstruct a deterministic 20,534-byte tar.gz archive at SHA-256 `34af9ef006daf135fe099c9e3a937b33f253e9fe54e9aa2cc821d0646f032aea`. Its uncompressed tar is 112,640 bytes at SHA-256 `442eefe745ebd809dde9d1a955f9043b49db949f281a03839313c5a5efc873a7` and expands to eleven repository-native source and test files.

The component consumes one exact local feature-reference update receipt and one separately authored observation authorization. It records the exact `origin` base and feature branch object identities while preserving local `HEAD`, references, live index, worktree status, and diff surfaces.

Static qualification passes 218 of 218 checks. The noncopyrighted adversarial campaign passes 124 of 124 cases. The exact downstream campaign passes 36 of 36 checks by feeding the observed values into the admitted leased-push operator's preflight and proving that a stale observation is rejected by the live exact-lease comparison.

Runtime success stops at `PASS_REMOTE_REFERENCE_STATE_OBSERVED_PUSH_AUTHORIZATION_WITHHELD` with `REMOTE_FEATURE_PUSH_AUTHORIZATION_WITHHELD`. Carrier reconstruction is not observation, observation is not push authorization, and neither operation pushes a remote, opens a pull request, promotes canon, or mutates graph state.
