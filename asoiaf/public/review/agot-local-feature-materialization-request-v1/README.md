# AGOT local-feature materialization request carrier

This directory carries the exact public-safe source package for `asoiaf-agot-local-feature-materialization-request-v1`. Seven ordered RFC 4648 base64 chunks reconstruct a deterministic 20,753-byte tar.gz archive at SHA-256 `6b8a84702e148bfe921fc1eaec37a24392b914856cc9a5fe3b90c6166072ef68`, expanding to ten repository-native source and test files.

The component follows the named-human disposition recorder admitted at `d5898d092ec4091395612ac0f4c555334acec21e`. It accepts only an exact `ADMIT_EXACT_PROPOSITION` receipt at `LOCAL_FEATURE_MATERIALIZATION_REQUEST_WITHHELD`, plus a separately authorized plan from a different named actor. It binds the exact receipt and plan bytes, proposition and source-paragraph digests, expected main commit, governed branch and paths, classifications, reconciliation keys, subject keys, graph relation, and exact ledger preimages.

A successful operation derives one canonical append row for each governed AGOT transaction ledger and computes exact append and postimage identities in memory. It persists the append rows and descriptors only. It does not write either postimage file, modify a checkout or index, create a commit, move a reference, push a remote, open a pull request, promote canon, or mutate the graph.

The static verifier passes 86 of 86 checks. The noncopyrighted adversarial campaign passes 36 of 36 cases. Real receipts, propositions, plans, requests, postimages, repository writes, runtime commits, reference updates, remote pushes, runtime pull requests, canon promotions, and graph mutations remain zero.

Runtime success stops at `PASS_LOCAL_FEATURE_MATERIALIZATION_REQUEST_SEALED_EXECUTION_WITHHELD` with `LOCAL_FEATURE_POSTIMAGE_MATERIALIZATION_WITHHELD`. Carrier reconstruction and repository qualification prove package identity and behavior only. They do not execute a real materialization request.
