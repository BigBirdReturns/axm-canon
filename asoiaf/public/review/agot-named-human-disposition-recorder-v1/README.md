# AGOT named-human disposition recorder carrier

This directory carries the exact public-safe `asoiaf-agot-named-human-disposition-recorder-v1.zip` package as one RFC 4648 base64 object. `CARRIER.json` fixes the carrier and decoded archive identities. `ADMISSION.json` binds the package to the admitted local named-human review workstation while retaining zero real private paragraphs, dispositions, proposition admissions, materialization requests, canon effects, and graph effects.

Repository reconstruction proves package identity and qualification only. It does not record a human disposition or authorize the next local-feature materialization request.
