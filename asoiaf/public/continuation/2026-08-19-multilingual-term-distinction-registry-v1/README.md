# Multilingual term distinction registry v1

This directory admits an exact base64 carrier for the public-safe derivative archive `asoiaf-multilingual-term-distinction-registry-v1.tar.zst`. The three ordered carrier chunks reconstruct exactly 17,026 archive bytes at SHA-256 `9e36b6fe0701c596501d183f522b8daafe8e7129942df4a0b17042b29d923744`. The archive itself is not committed under a binary package path, so the carrier is classified separately from the reconstructed archive.

The reconstructed package expands to fourteen files containing two controlled concepts, eight language communities, sixteen observed term forms, eight distinction rows, twelve public routes, twenty-one evidence records, sixteen exact-witness holds, fifty-six provenance relations, an offline reader, and qualification receipts. Its verifier passes 29 of 29 checks before packaging and after deterministic extraction. `CARRIER.json`, `REGISTRY.json`, and `ADMISSION.json` bind the carrier order, package identity, census, source-quality limits, absent-parent hold, and zero-authority boundary.

The controlled pairs are Spanish `Otros` / `Espectros`, Persian `آدرها` / `وایت ها`, French `Autres` / `Spectres`, Italian `Estranei` / `Non-morti`, Portuguese `Outros` / `Criaturas`, Russian `Иные` / `Упыри`, Turkish `Ötekiler` / `Wightlar`, and Chinese `异鬼` / `尸鬼`. These are observed community-language forms. No publisher translation authority, licensed-edition uniformity, or automatic cross-language equivalence is claimed.

The mechanism boundary separates the supernatural beings from the dead humans or animals animated under their power. Russian standing remains secondary because the cited primary community chain was robots-blocked. Chinese wight standing remains secondary, and Italian `Non-morti` is supported contextually rather than by a recovered dedicated term page. Every form therefore retains its own exact-edition and versioned-community hold.

The sealed predecessor `asoiaf-multilingual-term-audit-v0.1.tar.zst` remains absent at 21,839 bytes and SHA-256 `161a2cb7267b431cb47aaa469bec71bc15cc00cb0e5e94d3058e22806516cab9`. Matching its 2/8/16/8/12/21/16 census is integration-shape compatibility, not exact parent identity.

The controlling question is whether every form can be bound to an exact licensed edition and versioned community witness without allowing adaptation vocabulary, community recurrence, silent normalization, carrier custody, or count compatibility to impersonate translation authority or canon.
