# AGOT draft pull-request request packet carrier

This directory carries the exact source-reconstructed `asoiaf-agot-draft-pr-request-packet-v1.zip` package as one RFC 4648 base64 object. The decoded ZIP is 12,890 bytes at SHA-256 `5f3d5307dcab68f25de8ceaf9c9fdc303c9e74c29c274b34a41344b2f91b1c10` and expands to thirteen public-safe files.

The component follows the admitted exact-leased remote feature-push operator. It binds one exact remote-push receipt, repository identity, base and head branches and commits, the two governed ledger paths, transaction candidate, programme census, and a separately named request actor before sealing a deterministic draft pull-request request packet.

The carrier and repository test consume no real remote-push receipt and create no pull request. Draft creation, readiness, review, merge, canon, graph, and private-source effects remain zero.
