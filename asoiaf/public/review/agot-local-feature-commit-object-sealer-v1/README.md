# AGOT local feature commit-object sealer v1

This directory carries the exact public-safe package `asoiaf-agot-local-feature-commit-object-sealer-v1.zip` through 5 ordered base64 chunks. The package is 15,001 bytes at SHA-256 `05a5f2c30eba04718d5bde8048a52f0028732dfea9588b855a87abafa4167b79` and expands to 13 files.

The component accepts one exact reviewed two-path worktree change set, a named human diff-review authorization, and a different commit-object actor. It uses a temporary Git index to create one unreferenced commit object while leaving the live index, feature-branch reference, worktree postimages, remote, pull-request state, canon state, and graph state unchanged.

The verifier passes 53 of 53 checks and the noncopyrighted synthetic campaign passes 14 of 14 checks. Real reviewed worktrees, commit objects, live-index mutations, branch-reference updates, pushes, pull requests, canon promotions, and graph mutations remain zero.

The controlling question is whether a future real commit object binds the exact reviewed worktree change set without allowing object existence, carrier reconstruction, or successful CI to impersonate a branch update or repository admission.
