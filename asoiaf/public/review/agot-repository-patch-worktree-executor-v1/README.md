# AGOT repository patch worktree executor v1

This directory carries the exact public-safe package `asoiaf-agot-repository-patch-worktree-executor-v1.zip` as ordered base64 chunks. The package is 16,232 bytes at SHA-256 `02abfe23c19170796cda38750e80efa69cf090370dfb8f568f12e766ec704ff8` and expands to 12 files.

The component accepts one separately validated repository patch bundle and can apply its exact two postimages only to a clean named feature branch at the exact authorized base commit. It verifies the live index, worktree, branch, repository identity, target preimages, package manifest, validation receipt, and a separately named worktree executor.

The static verifier passes 54 of 54 checks. The noncopyrighted synthetic campaign passes 17 of 17 checks. The component writes no real worktree in this admission and creates no index mutation, commit, reference update, push, pull request, canon effect, or graph effect.

The controlling question is whether a future real patch application changes only the two exact governed ledger paths while preserving the clean live index, fixed HEAD, named feature branch, and every later Git and remote authority boundary.
