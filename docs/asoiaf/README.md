# The Narrow Wall

`docs/asoiaf/` is the public reader surface for the governed ASOIAF estate. It presents custody, source lanes, released-future witnesses, and a bounded research-question builder without publishing holder-controlled source text or granting automatic canon effect.

The site is static, dependency-free, keyboard accessible, responsive, and compatible with GitHub Pages. `data/atlas.json` is the controlling display object. The display reports sealed continuation-wave custody separately from repository materialization so a retained hash cannot impersonate admitted bytes.
