# AGOT pull-request merge executor v1

This repository surface carries the exact public-safe merge executor package. The reconstructed ZIP contains the contract, explicit merge-authorization template, preflight and execution operator, static verifier, noncopyrighted fake-`gh` campaign, Windows launcher, and qualification receipts.

The package is `asoiaf-agot-pull-request-merge-executor-v1.zip`, 14,981 bytes at SHA-256 `7d9ac9f7e63ea603ae14dc37aca972233c10e190a2fc8665652c0d9891bfae8b`. `CARRIER.json` fixes the RFC 4648 payload identity, Git blob identity, decoded byte count, decoded digest, and twelve-file expansion census.

The executor accepts only a separately validated merge-request packet and an explicit merge authorization. It rechecks the exact open, non-draft, approved, conflict-free pull request, successful checks, base and head commits, and the two governed ledger paths. The bounded write uses squash merge with exact head matching. Branch deletion, auto-merge, and administrative bypass remain forbidden.

A successful real operation would stop at `PASS_PULL_REQUEST_SQUASH_MERGED_POST_MERGE_ADMISSION_WITHHELD`. Carrier reconstruction, CI success, or a merge receipt cannot impersonate post-merge repository admission, canon effect, or graph effect. The exact upstream merge-request packet, validator receipt, and real authorization remain absent.
