# AGOT review transaction stager v1

This repository surface carries the exact public-safe package that converts one separately validated, text-free AGOT review intake into one deterministic transaction candidate. The package does not inspect the private corpus, display source prose, admit a repository transaction, execute a human decision, promote canon, or mutate the graph.

The reconstructed object is `asoiaf-agot-review-transaction-stager-v1.zip`, 19,005 bytes at SHA-256 `e07f0a31f7f87907b5a59af3ea49138de286c66134fb945ee8bb54488443d8e5`, expanding to thirteen files. Git stores the exact RFC 4648 base64 carrier recorded by `CARRIER.json`; the binary ZIP path remains absent.

The stager requires one valid intake, its exact workstation validation receipt, a separately signed staging authorization bound to both digests, and the exact forty-five-candidate register. The staging executor must be named and must differ from the reviewer. A successful output remains `PASS_TRANSACTION_CANDIDATE_SEALED_PENDING_REPOSITORY_ADMISSION`, `transactionExecuted: false`, `canonEffect: none`, and `graphEffect: none`.

The component verifier passes 46 of 46 checks and its noncopyrighted adversarial campaign passes 16 of 16 checks. The campaign exercises all six governed actions, idempotent replay, divergent-output refusal, actor separation, input self-digests, source-text-field refusal, and zero-effect boundaries without using the private corpus.

Carrier reconstruction proves package identity only. It does not consume a real validated intake, execute a review transaction, create a canon effect, or mutate the graph. The current real census remains zero at every downstream stage.

The upstream workstation is [`../agot-local-human-review-workstation-v1/`](../agot-local-human-review-workstation-v1/README.md). The controlling question is whether the first separately validated real intake can be staged without allowing validator success, carrier custody, deterministic identity, or repository admission to impersonate transaction execution.
