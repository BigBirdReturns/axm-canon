# AGOT reviewed-worktree branch-release operator carrier

This directory carries the exact public-safe source package for `asoiaf-agot-reviewed-worktree-branch-release-operator-v1`. Four ordered RFC 4648 base64 chunks reconstruct a deterministic 23,243-byte tar.gz archive at SHA-256 `fe7f0409bb6bbfaef035ccdb8b9398ce467df1f601c60f744194a7a7371d3ec2`, expanding to eleven repository-native source and test files.

The component follows the commit-object receipt adapter admitted through PR #82 at signed `main` commit `cb5fd57143737a00985c9564e334e4ec16a578a0`. It accepts one exact adapted `/1` receipt, its exact independent validation, and a separately authored release authorization. It verifies the reviewed feature worktree, clean live index, exact two-path bytes, unreferenced commit object, actor separation, and every no-effect boundary.

Successful execution atomically replaces only the current worktree's symbolic `HEAD` pseudoref with the exact reviewed base commit. The feature-branch reference, every other reference, HEAD reflog, live index, reviewed worktree bytes, and commit object must remain byte-identical. The operation stops at `PASS_REVIEWED_WORKTREE_BRANCH_RELEASED_REFERENCE_UPDATE_WITHHELD` with `LOCAL_FEATURE_REFERENCE_UPDATE_WITHHELD`.

Static qualification passes 181 of 181 checks. The noncopyrighted adversarial campaign passes 46 of 46 cases. The exact admitted adapter-to-release-to-feature-reference-updater campaign passes 43 of 43 checks and proves that the updater reaches guarded compare-and-swap preflight only after the separate HEAD-pseudoref release. Real receipt consumption, releases, reference updates, pushes, runtime pull requests, canon promotions, and graph mutations remain zero.
