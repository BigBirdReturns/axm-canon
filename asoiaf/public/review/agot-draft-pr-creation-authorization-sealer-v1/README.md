# AGOT draft PR creation authorization sealer carrier

Two ordered RFC 4648 chunks reconstruct the exact deterministic source package for `asoiaf-agot-draft-pr-creation-authorization-sealer-v1`. The package expands to nine component files plus one repository replay test, passes 111 static checks and 71 noncopyrighted adversarial cases, and retains `DRAFT_PULL_REQUEST_CREATION_WITHHELD`.

Carrier reconstruction does not consume a real request packet, seal a real creation authorization, inspect or modify a remote branch, open or ready a pull request, merge, promote canon, mutate graph state, or read private source.
